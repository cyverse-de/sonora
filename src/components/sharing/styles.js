export default (theme) => ({
    chip: {
        marginBottom: theme.spacing(1),
    },

    closeButton: {
        float: "right",
    },

    dividerGrid: {
        maxWidth: theme.spacing(2),
    },
});
