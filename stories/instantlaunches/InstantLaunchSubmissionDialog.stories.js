import React from "react";

import { InstantLaunchSubmissionDialog } from "components/instantlaunches";

export default {
    title: "Instant Launches / Submission Dialog",
};

export const InstantLaunchSubmissionDialogTest = () => {
    return <InstantLaunchSubmissionDialog open={true} />;
};
